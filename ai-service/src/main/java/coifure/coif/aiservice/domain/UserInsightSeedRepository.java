package coifure.coif.aiservice.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UserInsightSeedRepository extends JpaRepository<UserInsightSeedEntity, String> {
}
